# frozen_string_literal: true

IRB.conf[:SAVE_HISTORY] ||= 1000
IRB.conf[:HISTORY_FILE] ||= File.join(ENV['XDG_CACHE_HOME'] || File.join(ENV['HOME'], '.cache'),
                                      'irb_history')
